# ---- Heavy binaries: never in git (GB-scale) ----
downloads/
vm/
*.7z
*.iso
*.vdi
*.vmdk
*.vbox-prev
*.ova

# ---- Runtime logs & machine state ----
logs/
*.log

# ---- Sample evidence files (may contain lab captures) ----
pcaps/
samples/

# NOTE: config/supabase.json is committed as a placeholder — it may only ever
# hold the PUBLIC anon key (safe by design; RLS protects data). The service_role
# key must never exist in this repo or any browser code.

# ---- OS / editor noise ----
Thumbs.db
desktop.ini
~$*
